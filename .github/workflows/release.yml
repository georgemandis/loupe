name: Build and Release

on:
  push:
    branches: [main]
    tags: ['v*']
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'LICENSE'

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        include:
          - target: ''
            archive_suffix: macos-aarch64
            build_flags: ''
          - target: x86_64-macos
            archive_suffix: macos-x86_64
            build_flags: '-Dmacos-sdk=/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk'
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Build
        run: |
          rm -rf zig-out
          if [ -n "${{ matrix.target }}" ]; then
            zig build -Dtarget=${{ matrix.target }} ${{ matrix.build_flags }}
          else
            zig build
          fi

      - name: Package archive
        run: |
          VERSION="${{ github.ref_type == 'tag' && github.ref_name || format('dev-{0}', github.sha) }}"
          ARCHIVE_DIR="loupe-${VERSION}-${{ matrix.archive_suffix }}"
          mkdir -p "${ARCHIVE_DIR}"
          cp zig-out/bin/loupe "${ARCHIVE_DIR}/"
          cp zig-out/lib/libloupe.dylib "${ARCHIVE_DIR}/"
          tar czf "${ARCHIVE_DIR}.tar.gz" "${ARCHIVE_DIR}"
          echo "ARCHIVE=${ARCHIVE_DIR}.tar.gz" >> "$GITHUB_ENV"

      - uses: actions/upload-artifact@v4
        with:
          name: loupe-${{ matrix.archive_suffix }}
          path: ${{ env.ARCHIVE }}

  build-windows:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Build
        run: |
          rm -rf zig-out
          zig build -Dtarget=x86_64-windows

      - name: Package archive
        run: |
          VERSION="${{ github.ref_type == 'tag' && github.ref_name || format('dev-{0}', github.sha) }}"
          ARCHIVE_DIR="loupe-${VERSION}-windows-x86_64"
          mkdir -p "${ARCHIVE_DIR}"
          cp zig-out/bin/loupe.exe "${ARCHIVE_DIR}/"
          cp zig-out/bin/loupe.dll "${ARCHIVE_DIR}/"
          zip -r "${ARCHIVE_DIR}.zip" "${ARCHIVE_DIR}"
          echo "ARCHIVE=${ARCHIVE_DIR}.zip" >> "$GITHUB_ENV"

      - uses: actions/upload-artifact@v4
        with:
          name: loupe-windows-x86_64
          path: ${{ env.ARCHIVE }}

  release:
    if: github.ref_type == 'tag'
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
